server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /position/positions.yaml

clients:
  - url: http://201.23.67.250:3100/loki/api/v1/push

scrape_configs:
- job_name: register-asterisk
  static_configs:
  - targets:
      - localhost
    labels:
      job: console-register-homolog
      __path__: /var/log/asterisk/full
  pipeline_stages:
  - regex:
      expression: '^\[(?P<timestamp>[^\]]+)\] (?P<level>\w+)\[(?P<pid>\d+)\] (?P<module>[^:]+): (?P<message>.*)$'
  - labels:
      level:


